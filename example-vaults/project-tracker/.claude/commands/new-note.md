Create a new note: $ARGUMENTS

Follow the `/new-note` workflow from CLAUDE.md. Parse the title and any `in:<folder>` or `template:<type>` options from the arguments. Default folder is `notes/`. Apply required provenance frontmatter to the created file.
